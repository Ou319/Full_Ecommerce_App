import 'package:flutter/material.dart';

class Jacketes extends StatelessWidget {
  const Jacketes({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Jackets page"),);
  }
}